#include "main.h"

/**
 * _abs - check Main
 * @r: integer input
 * Description: a function that returns absolute value number
 * Return: Absolute value of number r
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
